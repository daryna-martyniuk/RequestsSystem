<UserControl x:Class="Requests.UI.Views.MyWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <UserControl.Resources>
        <!-- Локальний стиль для ширших вкладок -->
        <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItemStyle}">
            <Setter Property="MinWidth" Value="220"/>
            <!-- Збільшено ширину -->
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Filters -->
            <RowDefinition Height="*"/>
            <!-- Tabs -->
        </Grid.RowDefinitions>

        <!-- 1. ХЕДЕР -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Робочий простір" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Ваші поточні завдання та запити" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
            </StackPanel>
            <Button Grid.Column="1" Content="+ Новий запит" Command="{Binding CreateRequestCommand}" Style="{StaticResource ActionBtnStyle}" Width="150" Height="40"/>
        </Grid>

        <!-- 2. ПАНЕЛЬ ФІЛЬТРІВ -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,20" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <!-- Пошук з підказкою -->
                    <Grid Margin="0,0,15,0">
                        <TextBox x:Name="SearchBox" Width="250" Height="35" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Padding="5" Style="{StaticResource ModernTextBox}"/>
                        <TextBlock Text="🔍 Пошук..." IsHitTestVisible="False" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#BDC3C7">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <DatePicker SelectedDate="{Binding FilterStartDate}" Width="120" Height="35" Margin="0,0,10,0" ToolTip="Дата з..."/>
                    <TextBlock Text="—" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding FilterEndDate}" Width="120" Height="35" Margin="0,0,15,0" ToolTip="Дата по..."/>
                    <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding FilterStatus}" Width="150" Height="35" Padding="5" VerticalContentAlignment="Center"/>
                    <Button Content="✕" Command="{Binding ClearFiltersCommand}" Style="{StaticResource NavButtonStyle}" Margin="10,0,0,0" ToolTip="Скинути фільтри"/>
                </StackPanel>

                <Button Grid.Column="1" Content="↻ Оновити" Command="{Binding RefreshCommand}" Style="{StaticResource NavButtonStyle}"/>
            </Grid>
        </Border>

        <!-- 3. ВКЛАДКИ -->
        <TabControl Grid.Row="2" BorderThickness="0" Background="Transparent">

            <!-- 1. МОЇ ЗАВДАННЯ (Головна вкладка для виконавця) -->
            <TabItem Header="Мої завдання">
                <TabControl BorderThickness="0" Background="Transparent" Margin="0,10,0,0">
                    <TabItem Header="В роботі">
                        <DataGrid ItemsSource="{Binding MyActiveTasksView}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" RowHeight="50" BorderThickness="0" GridLinesVisibility="Horizontal">
                            <DataGrid.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                            </DataGrid.InputBindings>
                            <DataGrid.Columns>
                                <!-- ID Column removed -->
                                <DataGridTextColumn Header="Тема" Binding="{Binding Request.Title}" Width="*"/>
                                <DataGridTextColumn Header="Пріоритет" Binding="{Binding Request.Priority.Name}" Width="100"/>
                                <DataGridTemplateColumn Header="Статус" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="5" Padding="5" Background="#EBF5FB">
                                                <TextBlock Text="{Binding Status.Name}" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Дедлайн" Binding="{Binding Request.Deadline, StringFormat='dd.MM.yyyy'}" Width="100"/>
                                <DataGridTemplateColumn Header="Дії" Width="180">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}" ToolTip="Інформація"/>

                                                <!-- Пауза -->
                                                <Button Content="⏸" Command="{Binding DataContext.PauseTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Пауза">
                                                    <Button.Style>
                                                        <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                            <Setter Property="Background" Value="{StaticResource WarningOrange}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status.Name}" Value="На паузі">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>

                                                <!-- Відновити -->
                                                <Button Content="▶" Command="{Binding DataContext.ResumeTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Відновити">
                                                    <Button.Style>
                                                        <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                            <Setter Property="Background" Value="#3498DB"/>
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status.Name}" Value="На паузі">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>

                                                <Button Content="✓" Command="{Binding DataContext.CompleteTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource SuccessGreen}" ToolTip="Виконати"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="Архів">
                        <DataGrid ItemsSource="{Binding MyCompletedTasksView}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" RowHeight="50" BorderThickness="0">
                            <DataGrid.Columns>
                                <!-- ID Column removed -->
                                <DataGridTextColumn Header="Тема" Binding="{Binding Request.Title}" Width="*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status.Name}" Width="120"/>
                                <DataGridTextColumn Header="Завершено" Binding="{Binding CompletedAt, StringFormat='dd.MM.yyyy'}" Width="120"/>
                                <DataGridTemplateColumn Header="Дії" Width="60">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </TabItem>

            <!-- 2. МОЇ ЗАПИТИ (Я автор) -->
            <TabItem Header="Мої запити">
                <DataGrid ItemsSource="{Binding MyRequestsView}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="White" RowHeight="45" BorderThickness="0" GridLinesVisibility="Horizontal">
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenDetailsCommand}" CommandParameter="{Binding}"/>
                    </DataGrid.InputBindings>
                    <DataGrid.Columns>
                        <!-- ID Column removed -->
                        <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Категорія" Binding="{Binding Category.Name}" Width="150"/>
                        <DataGridTemplateColumn Header="Статус" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource BackgroundLight}" CornerRadius="10" Padding="10,2">
                                        <TextBlock Text="{Binding GlobalStatus.Name}" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Дедлайн" Binding="{Binding Deadline, StringFormat='dd.MM.yyyy'}" Width="100"/>
                        <DataGridTemplateColumn Header="Дії" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}" ToolTip="Переглянути"/>

                                        <!-- Кнопка Винести на обговорення (Тільки для керівника) -->
                                        <Button Content="💬" Command="{Binding DataContext.DiscussRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="На обговорення">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                    <Setter Property="Background" Value="#8E44AD"/>
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding DataContext.IsManager, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </MultiDataTrigger>
                                                        <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="Завершено">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Button Content="✎" Command="{Binding DataContext.EditRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource WarningOrange}" ToolTip="Редагувати"/>
                                        <Button Content="✕" Command="{Binding DataContext.DeleteRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource DangerRed}" ToolTip="Видалити"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- 3. ВХІДНІ (Керівник) -->
            <TabItem Header="Вхідні задачі" Visibility="{Binding ManagerVisibility}">
                <DataGrid ItemsSource="{Binding IncomingTasksView}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" RowHeight="50" BorderThickness="0">
                    <DataGrid.Columns>
                        <!-- ID Column removed -->
                        <DataGridTextColumn Header="Тема" Binding="{Binding Request.Title}" Width="*"/>
                        <DataGridTextColumn Header="Пріоритет" Binding="{Binding Request.Priority.Name}" Width="100"/>
                        <DataGridTextColumn Header="Автор" Binding="{Binding Request.Author.FullName}" Width="150"/>
                        <DataGridTemplateColumn Header="Дії" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                        <!-- Винести на обговорення (для керівника, який отримав задачу) -->
                                        <Button Content="💬" Command="{Binding DataContext.DiscussTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="#8E44AD" ToolTip="Винести на обговорення"/>
                                        <Button Content="👤" Command="{Binding DataContext.AssignExecutorCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource PrimaryBlue}" ToolTip="Призначити"/>
                                        <Button Content="➤" Command="{Binding DataContext.ForwardTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource WarningOrange}" ToolTip="Переслати"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- 4. НА ПОГОДЖЕННЯ (Керівник) -->
            <TabItem Header="На погодження" Visibility="{Binding ManagerVisibility}">
                <DataGrid ItemsSource="{Binding PendingApprovalsView}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" RowHeight="50" BorderThickness="0">
                    <DataGrid.Columns>
                        <!-- ID Column removed -->
                        <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Автор" Binding="{Binding Author.FullName}" Width="150"/>
                        <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat='dd.MM HH:mm'}" Width="120"/>
                        <DataGridTemplateColumn Header="Дії" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                        <Button Content="💬" Command="{Binding DataContext.DiscussRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="#8E44AD" ToolTip="Обговорити"/>
                                        <Button Content="✎" Command="{Binding DataContext.EditRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource WarningOrange}" ToolTip="Редагувати"/>
                                        <Button Content="✓" Command="{Binding DataContext.ApproveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource SuccessGreen}" ToolTip="Погодити"/>
                                        <Button Content="✕" Command="{Binding DataContext.RejectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource DangerRed}" ToolTip="Відхилити"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- 5. НА ОБГОВОРЕННІ (Керівник) -->
            <TabItem Header="На обговоренні" Visibility="{Binding ManagerVisibility}">
                <DataGrid ItemsSource="{Binding RequestsInDiscussion}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" RowHeight="50" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Автор" Binding="{Binding Author.FullName}" Width="150"/>
                        <DataGridTemplateColumn Header="Дії" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                        <!-- Кнопка ЗАВЕРШИТИ ОБГОВОРЕННЯ -->
                                        <Button Content="✅" Command="{Binding DataContext.FinishDiscussionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="#2E86C1" Width="80" ToolTip="Завершити"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>