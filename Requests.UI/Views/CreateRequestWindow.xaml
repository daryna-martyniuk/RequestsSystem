<Window x:Class="Requests.UI.Views.CreateRequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Новий запит" Height="800" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#3498DB"/>
        <SolidColorBrush x:Key="TextGray" Color="#7F8C8D"/>
        <SolidColorBrush x:Key="TextDark" Color="#2C3E50"/>
    </Window.Resources>

    <Border BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" CornerRadius="10" Background="White">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Заголовок -->
                <RowDefinition Height="*"/>
                <!-- Форма -->
                <RowDefinition Height="Auto"/>
                <!-- Кнопки -->
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Створення запиту" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" HorizontalAlignment="Center"/>
                <TextBlock Text="Заповніть деталі вашого звернення" FontSize="14" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                <Separator Margin="0,15,0,0" Background="#F0F0F0"/>
            </StackPanel>

            <!-- Форма -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
                <StackPanel>

                    <!-- Тема -->
                    <TextBlock Text="Тема запиту" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Категорія -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Категорія" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name" Margin="0,0,0,15" Padding="5"/>
                        </StackPanel>

                        <!-- Пріоритет -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Пріоритет" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Priorities}" SelectedItem="{Binding SelectedPriority}" DisplayMemberPath="Name" Margin="0,0,0,15" Padding="5"/>
                        </StackPanel>
                    </Grid>

                    <!-- Дедлайн (НОВЕ) -->
                    <TextBlock Text="Бажаний термін виконання (Дедлайн)" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding Deadline}" Margin="0,0,0,15" Padding="5" Height="35" VerticalContentAlignment="Center" Background="White"/>

                    <!-- Кому (Відділи) -->
                    <TextBlock Text="Виконавець (Оберіть один або кілька відділів)" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
                    <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="5" Margin="0,0,0,15" Height="100">
                        <ListBox ItemsSource="{Binding Departments}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" Margin="0,3" FontSize="14"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>

                    <!-- Опис -->
                    <TextBlock Text="Детальний опис" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                             Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                             VerticalContentAlignment="Top" Padding="10" Margin="0,0,0,15"/>

                    <!-- Вкладення (Drag & Drop зона) -->
                    <TextBlock Text="Вкладення" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>

                    <!-- Зона перетягування -->
                    <Border BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" CornerRadius="5" Padding="10" 
                            Background="#F8FBFE" AllowDrop="True" Drop="Border_Drop" Cursor="Hand" Margin="0,0,0,5"
                            PreviewMouseLeftButtonDown="Border_PreviewMouseLeftButtonDown">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📎 Натисніть або перетягніть файл сюди" Foreground="{StaticResource PrimaryBlue}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="(Макс 10 МБ)" FontSize="10" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Відображення вибраного файлу -->
                    <Border Background="#F0F9EB" CornerRadius="5" Padding="10,5" BorderBrush="{StaticResource SuccessGreen}" BorderThickness="1"
                            Visibility="{Binding HasAttachment, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <DockPanel>
                            <Button Command="{Binding RemoveFileCommand}" Content="✕" DockPanel.Dock="Right" 
                                    Background="Transparent" Foreground="#C0392B" FontWeight="Bold" BorderThickness="0" Margin="10,0,0,0"/>
                            <TextBlock Text="{Binding AttachmentName}" Foreground="{StaticResource SuccessGreen}" FontWeight="Bold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                        </DockPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Скасувати" Command="{Binding CancelCommand}" IsCancel="True" 
                        Background="Transparent" Foreground="{StaticResource TextGray}" BorderThickness="0" 
                        FontSize="14" FontWeight="SemiBold" Padding="15,5" Cursor="Hand"/>

                <Button Content="Створити запит" Command="{Binding SaveCommand}" 
                        Background="{StaticResource PrimaryBlue}" Foreground="White" BorderThickness="0"
                        FontSize="14" FontWeight="Bold" Padding="30,10" Margin="15,0,0,0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>