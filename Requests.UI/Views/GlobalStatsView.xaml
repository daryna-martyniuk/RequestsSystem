<UserControl x:Class="Requests.UI.Views.GlobalStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <UserControl.Resources>
        <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItemStyle}">
            <Setter Property="MinWidth" Value="220"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Effect="{StaticResource CardShadow}" Margin="0,0,20,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Співробітники" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{StaticResource TextPrimary}"/>
                <ListBox Grid.Row="1" ItemsSource="{Binding AllEmployees}" SelectedItem="{Binding SelectedEmployee}" BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="border" CornerRadius="5" Background="Transparent" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBlue}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#EBF5FB"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding FullName}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Position.Name}" FontSize="11" Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid Visibility="{Binding IsGlobalViewVisible}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Глобальна статистика" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Border Background="{StaticResource PrimaryBlue}" CornerRadius="5" Padding="15,10" Margin="0,0,15,0">
                            <StackPanel>
                                <TextBlock Text="Всього запитів" Foreground="White" Opacity="0.8" FontSize="12"/>
                                <TextBlock Text="{Binding TotalCount}" Foreground="White" FontWeight="Bold" FontSize="20"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource SuccessGreen}" CornerRadius="5" Padding="15,10">
                            <StackPanel>
                                <TextBlock Text="Завершено" Foreground="White" Opacity="0.8" FontSize="12"/>
                                <TextBlock Text="{Binding CompletedCount}" Foreground="White" FontWeight="Bold" FontSize="20"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,15" Effect="{StaticResource CardShadow}">
                    <WrapPanel Orientation="Horizontal">
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Період з:" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            <DatePicker SelectedDate="{Binding FilterStartDate}" Width="120" Height="30"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="по:" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            <DatePicker SelectedDate="{Binding FilterEndDate}" Width="120" Height="30"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Статус:" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            <ComboBox ItemsSource="{Binding FilterStatuses}" SelectedItem="{Binding SelectedStatusFilter}" Width="120" Height="30"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Пошук:" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="30" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        <Button Content="✕" Command="{Binding ClearFiltersCommand}" Style="{StaticResource NavButtonStyle}" Margin="10,0,0,0" ToolTip="Скинути фільтри"/>
                        <Button Content="📄 Звіт Директора" Command="{Binding GenerateReportCommand}" Style="{StaticResource ActionBtnStyle}" VerticalAlignment="Bottom" Height="30"/>
                    </WrapPanel>
                </Border>

                <Border Grid.Row="2" Background="White" CornerRadius="10" Effect="{StaticResource CardShadow}" Padding="10">
                    <DataGrid ItemsSource="{Binding GlobalRequestsView}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Автор" Binding="{Binding Author.FullName}" Width="150"/>
                            <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding GlobalStatus.Name}" Width="120"/>
                            <DataGridTemplateColumn Header="Дії" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                            <!-- Кнопка Обговорити -->
                                            <Button Content="💬" Command="{Binding DataContext.DiscussRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Обговорити">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                        <Setter Property="Background" Value="#8E44AD"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="На уточненні">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="Завершено">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="Скасовано">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <Grid Visibility="{Binding IsDetailViewVisible}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
                    <Button Content="⬅ Назад" Command="{Binding BackToGlobalCommand}" Style="{StaticResource ActionBtnStyle}" Background="{StaticResource TextSecondary}" Margin="0,0,20,0"/>
                    <StackPanel>
                        <TextBlock Text="{Binding SelectedEmployee.FullName}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="{Binding SelectedEmployee.Department.Name}" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>

                <TabControl Grid.Row="1">
                    <TabItem Header="Їхні Запити">
                        <DataGrid ItemsSource="{Binding EmployeeRequests}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding GlobalStatus.Name}" Width="120"/>
                                <DataGridTemplateColumn Header="Дії" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                                <!-- Кнопка Обговорити -->
                                                <Button Content="💬" Command="{Binding DataContext.DiscussRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Обговорити">
                                                    <Button.Style>
                                                        <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                            <Setter Property="Background" Value="#8E44AD"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="На уточненні">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="Завершено">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="Скасовано">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="Їхні Завдання">
                        <DataGrid ItemsSource="{Binding EmployeeTasks}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Запит" Binding="{Binding Request.Title}" Width="*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status.Name}" Width="120"/>
                                <DataGridTemplateColumn Header="Дії" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl>