<Window x:Class="Requests.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Requests.UI.ViewModels"
        mc:Ignorable="d"
        Title="Вхід у систему | Requests System" Height="550" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundLight}" WindowStyle="None" AllowsTransparency="True">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Border CornerRadius="15" Background="{StaticResource BackgroundLight}">
        <Grid>
            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="15"
                    Background="Transparent" Foreground="{StaticResource TextSecondary}"
                    FontSize="16" FontWeight="Bold" Cursor="Hand" BorderThickness="0"
                    Command="{Binding CloseCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource DangerRed}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Border Width="320" Height="420" Background="White" CornerRadius="10" Effect="{StaticResource CardShadow}" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,30" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="REQUESTS" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock Text=".SYS" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}"/>
                        </StackPanel>
                        <TextBlock Text="Система корпоративних запитів" FontSize="14" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                        <TextBlock Text="Логін" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15"
                                 FontSize="14" BorderBrush="{StaticResource BorderColor}"/>

                        <TextBlock Text="Пароль" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="UserPasswordBox" Padding="10" Margin="0,0,0,5"
                                     FontSize="14" BorderBrush="{StaticResource BorderColor}"/>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerRed}" 
                                   TextWrapping="Wrap" Margin="0,10,0,0" Height="35" FontSize="12"/>

                        <Button Content="УВІЙТИ" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=UserPasswordBox}"
                                Style="{StaticResource ActionBtnStyle}" Height="45" Margin="0,15,0,0"
                                FontSize="14" FontWeight="Bold"/>
                    </StackPanel>

                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>