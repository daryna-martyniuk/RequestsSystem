<Window x:Class="Requests.UI.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Requests.UI.ViewModels"
        xmlns:views="clr-namespace:Requests.UI.Views"
        mc:Ignorable="d"
        Title="Requests System" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen" Background="{StaticResource BackgroundLight}">

    <!-- Ресурси вже визначені в App.xaml, тому тут DataTemplate не обов'язкові, 
         але для Design-Time підтримки їх можна залишити або прибрати, якщо App.xaml працює коректно -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ХЕДЕР -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBackground}" Padding="20,10" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Лого -->
                    <ColumnDefinition Width="*"/>
                    <!-- Навігація -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Профіль -->
                </Grid.ColumnDefinitions>

                <!-- ЛОГО -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource PrimaryBlue}" CornerRadius="10" Width="40" Height="40" Margin="0,0,10,0">
                        <TextBlock Text="R" Foreground="White" FontWeight="Bold" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Requests" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>

                <!-- НАВІГАЦІЯ -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Мій простір" Command="{Binding ShowWorkspaceCommand}" Style="{StaticResource NavButtonStyle}"/>

                    <!-- АДМІН -->
                    <Button Content="Користувачі" Command="{Binding ShowUsersCommand}" Visibility="{Binding IsAdminVisible}" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Структура" Command="{Binding ShowStructureCommand}" Visibility="{Binding IsAdminVisible}" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Логи" Command="{Binding ShowLogsCommand}" Visibility="{Binding IsAdminVisible}" Style="{StaticResource NavButtonStyle}"/>

                    <!-- КЕРІВНИК (Статистика відділу) -->
                    <Button Content="Статистика відділу" Command="{Binding ShowDepartmentStatsCommand}" Visibility="{Binding IsManagerVisible}" Style="{StaticResource NavButtonStyle}"/>

                    <!-- ДЛЯ ДИРЕКТОРА (Глобальні звіти) -->
                    <!-- ВИПРАВЛЕНО: Command="{Binding ShowGlobalStatsCommand}" -->
                    <Button Content="Глобальна статистика" Command="{Binding ShowGlobalStatsCommand}" Visibility="{Binding IsDirectorVisible}" Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>

                <!-- ПРОФІЛЬ -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                    <StackPanel VerticalAlignment="Center" Margin="0,0,15,0">
                        <TextBlock Text="{Binding UserName}" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding UserRole}" FontSize="11" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Right"/>
                    </StackPanel>

                    <Button Content="⚙" Command="{Binding EditProfileCommand}" Style="{StaticResource NavButtonStyle}" Padding="10" ToolTip="Профіль"/>
                    <Button Content="Вихід" Command="{Binding LogoutCommand}" Style="{StaticResource ActionBtnStyle}" Background="{StaticResource DangerRed}" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="20" Background="{StaticResource CardBackground}" CornerRadius="10" Effect="{StaticResource CardShadow}">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</Window>