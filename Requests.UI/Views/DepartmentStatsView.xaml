<UserControl x:Class="Requests.UI.Views.DepartmentStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <UserControl.Resources>
        <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItemStyle}">
            <Setter Property="MinWidth" Value="220"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Effect="{StaticResource CardShadow}" Margin="0,0,20,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Моя Команда" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{StaticResource TextPrimary}"/>
                <ListBox Grid.Row="1" ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="border" CornerRadius="5" Background="Transparent" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBlue}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#EBF5FB"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding FullName}" FontWeight="SemiBold"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid Visibility="{Binding IsOverviewVisible}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Статистика відділу" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Border Background="#F39C12" CornerRadius="5" Padding="15,5" Margin="0,0,10,0">
                            <TextBlock Text="{Binding TotalActiveTasks, StringFormat='В роботі: {0}'}" Foreground="White" FontWeight="Bold"/>
                        </Border>
                        <Border Background="#27AE60" CornerRadius="5" Padding="15,5">
                            <TextBlock Text="{Binding TotalDoneTasks, StringFormat='Виконано: {0}'}" Foreground="White" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,15" Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Період з:" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding FilterStartDate}" Height="35" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="по:" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding FilterEndDate}" Height="35" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Статус:" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding FilterStatuses}" SelectedItem="{Binding SelectedStatusFilter}" Height="35" HorizontalAlignment="Stretch" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="6">
                            <TextBlock Text="Пошук:" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Height="35" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        <Button Grid.Column="8" Content="✕" Command="{Binding ClearFiltersCommand}" Style="{StaticResource NavButtonStyle}" ToolTip="Скинути фільтри" VerticalAlignment="Bottom" Height="35" Margin="0,0,0,0"/>
                        <Button Grid.Column="10" Content="📊 Звіт Відділу" Command="{Binding GenerateReportCommand}" Style="{StaticResource ActionBtnStyle}" Height="35" VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>

                <TabControl Grid.Row="2">
                    <TabItem Header="Всі завдання">
                        <Border Background="White" CornerRadius="10" Effect="{StaticResource CardShadow}" Padding="10" Margin="0,10,0,0">
                            <DataGrid ItemsSource="{Binding DepartmentTasksView}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Запит" Binding="{Binding Request.Title}" Width="*"/>
                                    <DataGridTextColumn Header="Статус" Binding="{Binding Status.Name}" Width="120"/>
                                    <DataGridTextColumn Header="Дата" Binding="{Binding AssignedAt, StringFormat=dd.MM.yyyy}" Width="100"/>
                                    <DataGridTextColumn Header="Виконавець" Binding="{Binding Executors[0].User.FullName, TargetNullValue='-'}" Width="150"/>

                                    <DataGridTemplateColumn Header="Дії" Width="180">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                                    <Button Content="👤" Command="{Binding DataContext.AssignExecutorCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource PrimaryBlue}"/>

                                                    <!-- Кнопка Обговорити -->
                                                    <Button Content="💬" Command="{Binding DataContext.DiscussTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Обговорити">
                                                        <Button.Style>
                                                            <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                                <Setter Property="Background" Value="#8E44AD"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Request.GlobalStatus.Name}" Value="На уточненні">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Request.GlobalStatus.Name}" Value="Завершено">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Request.GlobalStatus.Name}" Value="Скасовано">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <Button Content="⏸" Command="{Binding DataContext.PauseTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="#E67E22"/>
                                                    <Button Content="➤" Command="{Binding DataContext.ForwardTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="#9B59B6"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </TabItem>
                </TabControl>
            </Grid>

            <Grid Visibility="{Binding IsDetailVisible}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
                    <Button Content="⬅ Назад" Command="{Binding BackCommand}" Style="{StaticResource ActionBtnStyle}" Background="{StaticResource TextSecondary}" Margin="0,0,20,0"/>
                    <TextBlock Text="{Binding SelectedEmployee.FullName}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>

                <TabControl Grid.Row="1">
                    <TabItem Header="Завдання">
                        <DataGrid ItemsSource="{Binding SelectedEmployeeTasks}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Запит" Binding="{Binding Request.Title}" Width="*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status.Name}" Width="120"/>
                                <DataGridTemplateColumn Header="Дії" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                                <!-- Кнопка Обговорити -->
                                                <Button Content="💬" Command="{Binding DataContext.DiscussTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Обговорити">
                                                    <Button.Style>
                                                        <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                            <Setter Property="Background" Value="#8E44AD"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Request.GlobalStatus.Name}" Value="На уточненні">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Request.GlobalStatus.Name}" Value="Завершено">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Request.GlobalStatus.Name}" Value="Скасовано">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="Створені Запити">
                        <DataGrid ItemsSource="{Binding SelectedEmployeeRequests}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding GlobalStatus.Name}" Width="120"/>
                                <DataGridTemplateColumn Header="Дії" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="👁" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ActionIconBtnStyle}" Background="{StaticResource TextSecondary}"/>
                                                <!-- Кнопка Обговорити -->
                                                <Button Content="💬" Command="{Binding DataContext.DiscussRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Обговорити">
                                                    <Button.Style>
                                                        <Style TargetType="Button" BasedOn="{StaticResource ActionIconBtnStyle}">
                                                            <Setter Property="Background" Value="#8E44AD"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="На уточненні">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="Завершено">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding GlobalStatus.Name}" Value="Скасовано">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl>